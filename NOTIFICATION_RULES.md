# 消息通知规则

更新时间：2026-08-04

本文档描述当前有效的任务通知链路。系统采用“站内消息落库 + 飞书应用消息投递”；飞书投递失败不会回滚主业务，失败原因记录在通知表中。

## 渠道

- `in_app`：站内消息，保存通知内容、接收人和投递结果。
- `feishu_app`：飞书企业自建应用消息，用于通知具体员工。
- `feishu_bot`：群机器人消息，仅用于显式配置的群广播，不是任务主流程默认渠道。

## 角色来源

- 派发者：按基金读取 `customer_workflow_members(role_code = dispatcher)`。
- 一审人员：按业务大类读取 `business_category_review_members`。
- 二审人员：按基金读取 `customer_workflow_members(role_code = customer_reviewer)`。
- 执行人：读取 `tasks.assignee_user_id`。
- 创建人和实际派发人：分别读取 `tasks.created_by_user_id`、`tasks.dispatcher_user_id`，只参与审计或明确约定的结果通知。
- `tasks.reporter_user_id` 是历史兼容字段，不再决定当前通知接收人。

## 主流程通知

| 动作 | 接收人 | 消息入口 | 状态影响 |
| --- | --- | --- | --- |
| AI 预览候选待确认 | 对应基金的派发候选人 | 派发者工作台 | 不进入正式需求库 |
| 任务被指派或改派 | 当前执行人 | 项目资产页 | 任务进入 `assigned`，目录在派发时创建 |
| 执行人保存草稿 | 无通知 | 项目资产页 | 只更新 `task_directories.draft_payload_json` |
| 执行人提交交付 | 对应业务大类的全部一审候选人 | 资产审核页 | 任务进入 `pending_review/product_review` |
| 一审人员领取 | 无额外通知 | 资产审核页 | 一审工作项原子变为已领取，其他候选人只读 |
| 一审通过 | 对应基金的全部二审候选人 | 资产审核页 | 任务保持 `pending_review`，转为 `customer_review` |
| 二审人员领取 | 无额外通知 | 资产审核页 | 二审工作项原子变为已领取，其他候选人只读 |
| 二审通过 | 执行人、实际派发人和创建人，自动去重 | 无强制跳转 | 任务进入 `completed/done` |
| 一审或二审退回 | 执行人、实际派发人，自动去重 | 项目资产页 | 任务进入 `returned/execute` |
| 执行人催进度 | 当前审核阶段的候选团队 | 资产审核页 | 不改变任务状态 |
| 派发者催交付 | 当前任务执行人 | 任务资产页 | 不改变任务状态 |

同一工作项只能被一个候选人领取。领取和审核状态变更均使用数据库条件更新；并发处理失败时，后到请求会收到冲突提示。

## 任务链接

- 执行人消息直达 `asset-sheet.html`，用于保存草稿或提交正式交付。
- 审核人消息直达 `asset-review.html`，用于领取、通过或退回当前审核工作项。
- 公网部署必须配置 `APP_PUBLIC_URL`；消息中不得出现 `localhost`。
- 飞书链接会附带面向接收人的工作流身份交接参数，进入页面后按接收人账号建立会话。

## 去重与频控

- `notification_messages.idempotency_key` 具有唯一索引；提供幂等键的扫描或候选通知在并发执行时只落一条消息。
- AI 预览候选通知使用“候选需求 + 派发者”作为幂等范围。
- 执行人催进度按“任务 + 执行人 + 审核阶段”限制 6 小时内一次，记录在 `task_progress_reminders`。
- 派发者催交付按“任务 + 派发人 + `execution_submission` 阶段”限制 6 小时内一次，记录在 `task_progress_reminders`；通知同时写入站内消息并尝试投递飞书应用消息。
- 任务工作项领取由数据库原子更新保证，不依赖前端按钮禁用。
- 新增通知场景必须明确幂等键；定时扫描上线前应先补齐对应场景的去重规则。

## 扫描能力

当前以下能力提供手动扫描接口，尚未接入统一调度器：

- 任务即将到期或逾期扫描。
- 飞书同步失败扫描。
- 资产地址缺失扫描。
- 任务进度反馈扫描。

工时未提交提醒目前禁用。定时任务上线时，应在单实例调度或分布式锁、数据库幂等键和投递监控均具备后再开启。

## 投递失败

- 站内消息保存后再尝试飞书投递。
- 飞书失败不会阻断指派、提交或审核状态转换。
- 投递结果写入 `delivery_result_json`，错误摘要写入 `error_message`。
- 管理员应通过失败扫描或监控定位权限、Open ID、公网地址和飞书接口错误。
